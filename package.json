{
  "name": "zoroasterverse",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "cd apps/frontend && npm install && npm run build"
  },
  "dependencies": {
    "framer-motion": "^12.23.12",
    "marked": "^16.2.0",
    "quill": "^2.0.3"
  }
}
